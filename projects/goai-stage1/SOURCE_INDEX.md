# 第一阶段源代码与制品索引

第一阶段的可读源码、模型台账和历史文档位于私有 Drive 包 `goai-stage1-m12-promoted-20260831` 中。包内保留原 `Project/` 相对路径，解压到 `~/Project` 后可访问：

* `GOAI_离线实验与M12资料包_20260816/`：发布源码、README、模型 ledger、历史解释；
* `go-ai/deliverables/GOAI-M12.0_submission_candidate_20260815.zip`：最终提交候选；
* M2/M6/M9/OP3 的 16 个 checkpoint：与离线资料包内 manifest 交叉校验。

已拒绝的 M10 transfer、RNA transfer 的大 OOF 和 batch-audit NPZ 不在该包中。它们的项目存在、结论和原服务器来源已在根 manifest/蓝图中记录；只有重开历史审计时才应单独取回。
